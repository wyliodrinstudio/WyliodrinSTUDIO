async function loadPlugins (progress = () => {}) {
	var plugins = [];
	var index = 0;

	let plugin0 = import ('../plugins/dashboard/dashboard/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'dashboard', consumes:["workspace","projects"], provides:["dashboard"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/dashboard', index, 50); });
	let plugin1 = import ('../plugins/dashboard/graph.extra/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.extra', consumes:["workspace","dashboard"], provides:["dashboard_graph_extra"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.extra', index, 50); });
	let plugin2 = import ('../plugins/dashboard/graph.gauge/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.gauge', consumes:["workspace","dashboard"], provides:["dashboard_graph_gauge"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.gauge', index, 50); });
	let plugin3 = import ('../plugins/dashboard/graph.line/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.line', consumes:["workspace","dashboard"], provides:["dashboard_graph_line"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.line', index, 50); });
	let plugin4 = import ('../plugins/dashboard/graph.slider/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.slider', consumes:["workspace","dashboard"], provides:["dashboard_graph_slider"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.slider', index, 50); });
	let plugin5 = import ('../plugins/dashboard/graph.speedometer/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.speedometer', consumes:["workspace","dashboard"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.speedometer', index, 50); });
	let plugin6 = import ('../plugins/dashboard/graph.switch/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.switch', consumes:["workspace","dashboard"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.switch', index, 50); });
	let plugin7 = import ('../plugins/dashboard/graph.thermometer/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.thermometer', consumes:["workspace","dashboard"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.thermometer', index, 50); });
	let plugin8 = import ('../plugins/dashboard/graph.vumeter/index.js').then ((plugin) => { plugins.push ({folder: 'dashboard', name:'graph.vumeter', consumes:["workspace","dashboard"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('dashboard/graph.vumeter', index, 50); });
	let plugin9 = import ('../plugins/devices/pinlayout/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'pinlayout', consumes:["workspace"], provides:["pin_layout"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/pinlayout', index, 50); });
	let plugin10 = import ('../plugins/devices/rpk/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'rpk', consumes:["workspace","projects","events","device_wyapp","console","filesystem","system"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/rpk', index, 50); });
	let plugin11 = import ('../plugins/devices/shell/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'shell', consumes:["workspace","xterm"], provides:["shell"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/shell', index, 50); });
	let plugin12 = import ('../plugins/devices/wyapp/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp', consumes:["workspace","events","console","projects","shell","notebook","dashboard","filesystem"], provides:["device_wyapp"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp', index, 50); });
	let plugin13 = import ('../plugins/devices/wyapp.device.beagleboneblack/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp.device.beagleboneblack', consumes:["device_wyapp","workspace","events","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp.device.beagleboneblack', index, 50); });
	let plugin14 = import ('../plugins/devices/wyapp.device.picopi/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp.device.picopi', consumes:["device_wyapp","workspace","events","editor_visual","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp.device.picopi', index, 50); });
	let plugin15 = import ('../plugins/devices/wyapp.device.raspberrypi/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp.device.raspberrypi', consumes:["device_wyapp","workspace","events","editor_visual","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp.device.raspberrypi', index, 50); });
	let plugin16 = import ('../plugins/devices/wyapp.device.udooneo/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp.device.udooneo', consumes:["device_wyapp","workspace","events"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp.device.udooneo', index, 50); });
	let plugin17 = import ('../plugins/devices/wyapp.websocket/index.js').then ((plugin) => { plugins.push ({folder: 'devices', name:'wyapp.websocket', consumes:["workspace","settings","device_wyapp"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('devices/wyapp.websocket', index, 50); });
	let plugin18 = import ('../plugins/documentation/readthedocs/index.js').then ((plugin) => { plugins.push ({folder: 'documentation', name:'readthedocs', consumes:["workspace","system"], provides:["documentation"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('documentation/readthedocs', index, 50); });
	let plugin19 = import ('../plugins/documentation/resistorcolorcodes/index.js').then ((plugin) => { plugins.push ({folder: 'documentation', name:'resistorcolorcodes', consumes:["workspace"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('documentation/resistorcolorcodes', index, 50); });
	let plugin20 = import ('../plugins//first.run/index.js').then ((plugin) => { plugins.push ({folder: '', name:'first.run', consumes:["events","settings","projects","system","workspace"], provides:["firstrun"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('/first.run', index, 50); });
	let plugin21 = import ('../plugins/languages/nodejs/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'nodejs', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/nodejs', index, 50); });
	let plugin22 = import ('../plugins/languages/python/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'python', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/python', index, 50); });
	let plugin23 = import ('../plugins/languages/rust/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'rust', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/rust', index, 50); });
	let plugin24 = import ('../plugins/languages/shell/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'shell', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/shell', index, 50); });
	let plugin25 = import ('../plugins/languages/visual/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual', index, 50); });
	let plugin26 = import ('../plugins/languages/visual.adafruitdht/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.adafruitdht', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.adafruitdht', index, 50); });
	let plugin27 = import ('../plugins/languages/visual.adafruitlcd/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.adafruitlcd', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.adafruitlcd', index, 50); });
	let plugin28 = import ('../plugins/languages/visual.gpiozero/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.gpiozero', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.gpiozero', index, 50); });
	let plugin29 = import ('../plugins/languages/visual.libwyliodrin/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.libwyliodrin', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.libwyliodrin', index, 50); });
	let plugin30 = import ('../plugins/languages/visual.rpk/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.rpk', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.rpk', index, 50); });
	let plugin31 = import ('../plugins/languages/visual.social/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.social', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.social', index, 50); });
	let plugin32 = import ('../plugins/languages/visual.wyapp/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.wyapp', consumes:["editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.wyapp', index, 50); });
	let plugin33 = import ('../plugins/languages/visual.wyliozero/index.js').then ((plugin) => { plugins.push ({folder: 'languages', name:'visual.wyliozero', consumes:["workspace","projects","editor_visual"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('languages/visual.wyliozero', index, 50); });
	let plugin34 = import ('../plugins/projects/editor.ace/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'editor.ace', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/editor.ace', index, 50); });
	let plugin35 = import ('../plugins/projects/editor.images/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'editor.images', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/editor.images', index, 50); });
	let plugin36 = import ('../plugins/projects/editor.monaco/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'editor.monaco', consumes:["workspace","projects"], provides:[], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/editor.monaco', index, 50); });
	let plugin37 = import ('../plugins/projects/editor.opcuamodel/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'editor.opcuamodel', consumes:["workspace","projects","editor_visual"], provides:["editor_opcuamodel"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/editor.opcuamodel', index, 50); });
	let plugin38 = import ('../plugins/projects/editor.visual/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'editor.visual', consumes:["workspace","projects"], provides:["editor_visual"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/editor.visual', index, 50); });
	let plugin39 = import ('../plugins/projects/notebook/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'notebook', consumes:["workspace","projects","filesystem"], provides:["notebook"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/notebook', index, 50); });
	let plugin40 = import ('../plugins/projects/projects/index.js').then ((plugin) => { plugins.push ({folder: 'projects', name:'projects', consumes:["workspace","xterm","filesystem","settings"], provides:["projects"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('projects/projects', index, 50); });
	let plugin41 = import ('../plugins/simulators/raspberrypi/index.js').then ((plugin) => { plugins.push ({folder: 'simulators', name:'raspberrypi', consumes:["workspace","console","projects"], provides:["device_simulator_raspberrypi"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('simulators/raspberrypi', index, 50); });
	let plugin42 = import ('../plugins/simulators/rpk/index.js').then ((plugin) => { plugins.push ({folder: 'simulators', name:'rpk', consumes:["workspace","console","projects"], provides:["device_simulator_rpk"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('simulators/rpk', index, 50); });
	let plugin43 = import ('../plugins/studio/console/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'console', consumes:["workspace","xterm"], provides:["console"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/console', index, 50); });
	let plugin44 = import ('../plugins/studio/events/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'events', consumes:[], provides:["events"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/events', index, 50); });
	let plugin45 = import ('../plugins/studio/filesystem.web/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'filesystem.web', consumes:[], provides:["filesystem"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/filesystem.web', index, 50); });
	let plugin46 = import ('../plugins/studio/settings/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'settings', consumes:["filesystem"], provides:["settings"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/settings', index, 50); });
	let plugin47 = import ('../plugins/studio/system.browser/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'system.browser', consumes:[], provides:["system"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/system.browser', index, 50); });
	let plugin48 = import ('../plugins/studio/workspace/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'workspace', consumes:["system","settings","hooks"], provides:["workspace"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/workspace', index, 50); });
	let plugin49 = import ('../plugins/studio/xterm/index.js').then ((plugin) => { plugins.push ({folder: 'studio', name:'xterm', consumes:["workspace"], provides:["xterm"], setup: plugin.setup || plugin.default || plugin}); index=index+1; progress ('studio/xterm', index, 50); });
	await Promise.all ([plugin0, plugin1, plugin2, plugin3, plugin4, plugin5, plugin6, plugin7, plugin8, plugin9, plugin10, plugin11, plugin12, plugin13, plugin14, plugin15, plugin16, plugin17, plugin18, plugin19, plugin20, plugin21, plugin22, plugin23, plugin24, plugin25, plugin26, plugin27, plugin28, plugin29, plugin30, plugin31, plugin32, plugin33, plugin34, plugin35, plugin36, plugin37, plugin38, plugin39, plugin40, plugin41, plugin42, plugin43, plugin44, plugin45, plugin46, plugin47, plugin48, plugin49, ])
	progress ('Your workspace is almost ready ...');
	return plugins;
}
module.exports.loadPlugins = loadPlugins;
